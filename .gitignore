# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build
/.next
/out

# uploaded media — the database stores the path, the bytes are not source
/public/uploads

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# playwright-mcp session artifacts
/.playwright-mcp

# alternate build output used for previewing while dev runs
/.next-preview
